/**
 * @param {number} n
 * @return {boolean}
 */
var checkDivisibility = function (n) {
    summation = 0 
    
};


/*
/**
 * @param {number} n
 * @return {boolean}
 *
var checkDivisibility = function (n) {
    digit_product = 1
    digit_sum = 0

    for (i of n.toString()) {
        num = Number(i)
        digit_sum += num
        digit_product *= num
    }

    sum = digit_product + digit_sum

    return n % sum == 0
};
 */